//@ts-nocheck

// Yup
// import * as Yup from 'yup';

// Types
// import { CurrenciesListItemType } from '../../interfaces';

// // Currencies List
// import { currenciesList } from '../../utils';

// const [btc, eth] = currenciesList;

// export const validationSchema = Yup.lazy((values) => {
//   return Yup.object({
//     sendAmount: Yup.number()
//       .min(minSendAmount, `Minimum amount is ${minSendAmount} ${sendCurrency}`)
//       .typeError('Must be a number!'),
//     getAmount: Yup.number().typeError('Must be a number!'),
//   });
// });

// export const initialValues = {
//   sendCurrency: btc.ticker.toUpperCase(),
//   sendAmount: 0,
//   getCurrency: eth.ticker.toUpperCase(),
//   getAmount: 0,
// };
